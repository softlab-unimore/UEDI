
def main():
    # Step 1: Get data
    # ToDo: get list of N sources
    #   1. get list of files inside a given folder
    #   2. (optional) get the integration ground truth in ER format file (see Magellan examples)
    #   2bis. (optional) get the integration ground truth like integrated DataFrames (final result)
    #   3. compute the integrated ground truth DataFrame

    # Step 2: Integration
    # ToDo: iterative integration of input sources
    #   option 1. compute ER results using the selected algorithm
    #   option 2. get ER results using the ground truth results

    # Step 3: Metrics computation
    # ToDo: compute the selected metrics for the new integrated, the current source, and/or the previous sources

    # Step 4: Visualize metrics
    # ToDo: plot metrics results

    pass


if __name__ == '__main__':
    main()
